# Example configuration file for tmb-import
# Copy this file to config.yaml and fill in your sensitive values.

# Git Identity for Automated Commits
GIT_USER_EMAIL: "someone@example.com"
GIT_USER_NAME: "Someone"

# Discord bot token for authentication with Discord
DISCORD_TOKEN: "YOUR_DISCORD_TOKEN_HERE"

# URL for exporting TMB data (JSON format)
EXPORT_DATA_URL: "https://thatsmybis.com/22344/best-friends/export/characters-with-items/html"

# URL for exporting TMB items (CSV format, optional)
# EXPORT_ITEMS_URL: "https://thatsmybis.com/19497/best-friends/export/item-notes/html"

# Discord webhook URL for post-import notifications.
# Provided to the container via the DISCORD_WEBHOOK_URL environment variable
# (homelab .env convention — never commit it). If unset, the import simply
# skips the Discord notification.
